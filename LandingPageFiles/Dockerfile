# Use a lightweight Nginx server
FROM nginx:alpine

# Copy your local HTML/CSS files into the container's web folder
COPY index.html /usr/share/nginx/html/index.html

# Expose the standard web port
EXPOSE 80