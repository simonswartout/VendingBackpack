# Backend API Documentation

The maintained operational API documentation now lives in [Docs/BACKEND_API.md](/Users/crimsonwheeler/Documents/GitHub/VendingBackpack/Docs/BACKEND_API.md).

That document reflects the current Rails + SQL operational contract, the seeded local Docker flow, the canonical `camelCase` DTO shapes, and the current production release bias toward `Frontend-Next`.
