# CHANGE: "Clean Lab" Visual Overhaul

## Goal
Transform the application's visual identity into a "Clean Lab" aesthetic: high-key minimalism, monospaced numbers, geometric sans-serif labels, and a left-side navigation rail.

## Status
- [x] Theme: Define `AppColors` and `AppTheme` constants (Done)
- [x] Base: Update `main.dart` with the new theme (Done)
- [x] Layout: Refactor `PagesLayout.dart` / `OverviewScreens.dart` for side rail navigation (Done)
- [x] Modules: Update `Dashboard`, `Warehouse`, and `Routes` views (Done)
- [x] Polish: Implement pulse animations and skeleton loaders (Added status pulses, using standard loaders)
